# 2026-04-17

---

## Hero 섹션 레이아웃 변경 (`app/page.tsx`)

### 변경 전 (4/13 기준)
- 텍스트 + 버튼을 화면 정중앙 배치 (`text-center`, `justify-center`)
- 배경 그래픽만 존재 (IntelGraph, 방사형 그라데이션)
- 최대 너비 `max-w-5xl`

### 변경 후
- **좌우 2분할 레이아웃** 도입 (데스크탑 `lg` 기준)
  - 좌측: 타이틀 + 설명 + CTA 버튼 그룹 (텍스트 좌정렬 `lg:text-left`)
  - 우측: `HrFlowDiagram` 컴포넌트 고정 배치 (`hidden lg:block`, `w-[360px] xl:w-[420px]`)
- 최대 너비 `max-w-5xl` → `max-w-6xl`로 확대
- 버튼 그룹 정렬: `justify-center` → `lg:justify-start`

### HrFlowDiagram 컴포넌트 신규 추가 (`components/hr-flow-diagram.tsx`)
- 채용 → 배치 → 육성 → 분석 HR 프로세스 흐름을 SVG S커브로 시각화
- 비율 고정 컨테이너 (`position: relative`, `paddingBottom: 133.33%`) + SVG `viewBox="0 0 300 400"`
- 카드 클릭 시 활성 상태 표시 (`useState`)
- 데스크탑에서만 렌더링 (모바일 미노출)

---

## Hero 원복 방법

> Hero 레이아웃을 4/13 이전 상태(텍스트 중앙 배치, HrFlowDiagram 없음)로 돌려야 할 경우

### 방법 1 — git으로 `app/page.tsx` 전체 원복 (빠르지만 SiteHeader 분리도 함께 되돌아감)

```bash
git checkout ff5f2f5 -- app/page.tsx
```

- `ff5f2f5` 시점의 page.tsx는 SiteHeader/SiteFooter가 분리되기 전 인라인 헤더/푸터 포함
- 원복 후 SiteHeader, SiteFooter import를 다시 추가하거나, 인라인 헤더를 그대로 사용해야 함

### 방법 2 — Hero 섹션만 수동으로 되돌리기 (SiteHeader 분리 유지하면서 원복)

`app/page.tsx` HERO 섹션의 `<div className="relative z-[3]...">` 블록을 아래로 교체:

```tsx
<div className="relative z-[3] max-w-5xl mx-auto px-5 sm:px-8 w-full text-center">
  <FadeIn delay={200}>
    <h1 className="text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-bold leading-[1.1] tracking-tight mb-8 text-slate-900">
      멈춰있는 역량 모델,<br />
      이제 <span className="text-[#1e4fa8]">실시간으로</span><br />
      진화합니다.
    </h1>
  </FadeIn>
  <FadeIn delay={360}>
    <p className="text-base sm:text-lg text-slate-500 leading-relaxed mb-10 max-w-2xl mx-auto px-2 sm:px-0">
      우리 조직만의 &apos;성공 공식&apos;을 데이터로 증명하세요. 보안이 설계된 전용 AI가 채용부터 리더십 코칭까지, 파편화된 인사 데이터를 하나의 성장 엔진으로 연결하여 최적의 HR 의사결정 시스템을 구축합니다.
    </p>
  </FadeIn>
  <FadeIn delay={500}>
    <div className="flex flex-col sm:flex-row gap-4 justify-center">
      <button className="cursor-pointer inline-flex items-center justify-center gap-2 bg-[#0f2d6e] hover:bg-[#1e4fa8] text-white font-semibold px-10 h-14 min-w-[230px] rounded-xl text-base transition-colors shadow-lg shadow-blue-900/20">
        서비스 소개서 다운로드
        <svg width="17" height="17" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M7 10l5 5 5-5M12 15V3" /></svg>
      </button>
      <button onClick={() => scrollTo("solution")} className="cursor-pointer relative z-10 inline-flex items-center justify-center gap-2 bg-white border border-slate-300 text-slate-700 hover:bg-slate-50 font-semibold px-10 h-14 min-w-[230px] rounded-xl text-base transition-colors">
        솔루션 알아보기
        <svg width="17" height="17" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path d="M5 12h14M12 5l7 7-7 7" /></svg>
      </button>
    </div>
  </FadeIn>
</div>
```

- `HrFlowDiagram` import 및 사용 코드도 함께 제거
- `import { HrFlowDiagram } from "@/components/hr-flow-diagram"` 줄 삭제

---

## 헤더 리팩터링 (`components/site-header.tsx`)

### 로고 이미지 전환
- 텍스트 "Piccle" → `<Image src="https://img.assesta.com/piccle/logo.png">` 로 교체
- `next/image` Image 컴포넌트 사용, `width={100}`

### 메인 페이지 전용 섹션 네비게이션
- `usePathname`으로 현재 경로 감지, `isMain` 조건 추가
- 메인(`/`)일 때만 문제제기/솔루션/활용사례/보안정책 네비 메뉴 렌더링
- 서브 페이지(서비스 소개, 문의하기 등)에서는 섹션 네비 미노출
- `scrollTo(id)` 함수 SiteHeader 내부로 이동 (이전: page.tsx에 있었음)

### 기타
- 문의하기 버튼 클릭: `router.push` → `window.location.href` (Next.js 라우터 대신 직접 이동)
- `MAIN_NAV` 배열 SiteHeader 내부로 이동 (이전: page.tsx에 있었음)

---

## 랜딩 페이지 구조 정리 (`app/page.tsx`)

- 인라인 헤더 제거 → `<SiteHeader />` 컴포넌트 사용으로 통일
- 인라인 푸터 제거 → `<SiteFooter />` 컴포넌트 사용으로 통일
- `scrollY` state 제거 (SiteHeader가 항상 고정 불투명 스타일 적용하므로 불필요)
- `NAV` 배열 제거 (SiteHeader 내부의 `MAIN_NAV`으로 통합)

---

## 푸터 정보 수정 (`components/site-footer.tsx`)

- 텍스트 회사명 → 로고 이미지(`logo_wh.png`)로 교체
- 사업자등록번호 수정: `107-86-27487` → `107-86-76668`
- 통신판매업신고 수정: `2013-서울영등포-0153` → `2022-서울영등포-0184`
- FAX 포맷 수정: `+82-787-1408` → `(02)787-1408`
- 카피라이트 수정: `ASSESTA` → `ASSESTAHRC`
- 소셜 아이콘(Instagram, KakaoTalk, YouTube) 주석 처리 (추후 계정 확정 시 주석 해제)

---

## 문의하기 페이지 타이틀/배지 수정 (`app/(sub)/inquiry/page.tsx`)

- 배지 텍스트: `Survey` → `Contact`
- h1 타이틀: `빠른 상담 접수` → `문의하기` (헤더 버튼·URL `/inquiry`와 일관성 확보)
- 개인정보처리방침 내용 수정: 서비스명 표현 정정, 개인정보 관리자 정보 업데이트

---

## 파비콘 및 이미지 추가

- `app/layout.tsx`: 파비콘 링크 추가 (`https://img.assesta.com/piccle/favicon.png`)
- `public/images/`: favicon.png, favicon_wh.png, logo.png, logo_wh.png 로컬 복사본 추가
- `next.config.mjs`: `img.assesta.com` remotePatterns 추가 (Next.js Image 외부 도메인 허용)

---

## IntelGraph 컴포넌트 간소화 (`components/intel-graph.tsx`)

- 노드 애니메이션(60개 이동 파티클) 제거
- 마우스 트래킹(커서 근처 노드 끌림 효과) 제거
- 정적 그리드 배경만 유지 (성능 개선)
